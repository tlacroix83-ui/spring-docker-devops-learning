spring:
  application:
    name: docker-demo
  datasource:
    url: jdbc:h2:mem:testdb;
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop